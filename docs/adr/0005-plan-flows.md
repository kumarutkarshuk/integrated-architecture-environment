# Stack Flows as separate clusters

A Plan may contain several Flows. Each Flow is its own subgraph, so insert and retrieve can both show Client → API → Store. Layout stacks Flows as titled clusters. Shared boxes with tagged arrows were rejected because two paths cannot share the same from/to pair, and one graph would mix the paths.
