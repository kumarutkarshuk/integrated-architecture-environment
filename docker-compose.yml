services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: iae
      POSTGRES_PASSWORD: iae
      POSTGRES_DB: iae
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U iae -d iae"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
